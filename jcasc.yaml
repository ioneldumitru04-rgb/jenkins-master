jenkins:
  systemMessage: "JENKINS MASTER START"

  numExecutors: 0

  clouds:
    - docker:
        name: "docker"
        dockerApi:
          dockerHost:
            uri: "unix:///var/run/docker.sock"
        templates:
          - labelString: "docker-agent"
            name: "docker-agent"
            dockerTemplateBase:
              image: "jenkins/inbound-agent:latest"
              tty: true
            remoteFs: "/home/jenkins/agent"
            instanceCapStr: "10"
            retentionStrategy:
              idleMinutes: 5

  globalNodeProperties:
    - envVars:
        env:
          - key: "DOCKER_HOST"
            value: "unix:///var/run/docker.sock"

unclassified:
  location:
    url: "https://exodermal-corrin-nonempirically.ngrok-free.dev" # change with ngrok or host's ip
appearance:
  themeManager:
    disableUserThemes: true
    theme: "darkSystem"

